{
  "name": "menufic-design-prototype",
  "private": true,
  "version": "0.0.0",
  "description": "Menufic V2 design prototype — Astro + React app in ./astro (migrated from the retired static HTML in ./landing).",
  "scripts": {
    "dev": "npm --prefix astro run dev",
    "build": "npm --prefix astro run build",
    "preview": "npm --prefix astro run preview",
    "check": "npm --prefix astro exec astro check"
  }
}
