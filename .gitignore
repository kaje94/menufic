# Root ignore — repo-level only. The legacy app has its own apps/legacy-webapp/.gitignore.

# AI / editor tooling — not part of the deliverable
.agents/
.claude/
.codex/
.cursor/
.impeccable/
.opencode/
.superpowers/

# Scratch / generated verification artifacts
.shots/
*.log
TEMP_REQ.md
SESSION.md

# Dependencies / secrets / OS cruft
node_modules/
.env
.env*.local
.DS_Store
